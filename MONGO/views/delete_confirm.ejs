<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Chat</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h3>Are you sure to delete?</h3>
    <div class="chat">
        <p>From: <i><%=chat.from%></i></p>
        <div class="msg">
            <p><%=chat.msg%></p>
            
        </div>
        <p>Recieved by: <i><%=chat.to%></i></p>
        <hr>
        <p><%=chat.created_at.toString().split(" ")[4]%></p>
        <p><%=chat.created_at.toString().split(" ").slice(0,4).join(" ")%></p>
    </div>
    <br>
    <form action="/chats/<%=chat.id%>/delete_yes?_method=Delete" method="post" style="display: inline;">
        <button>Yes</button>
    </form>
    &nbsp;&nbsp;&nbsp;
    <form action="/chats" method="get" style="display: inline;">
        <button>No</button>
    </form>
</body>
</html>